{
  "name": "jenkins-demo-app",
  "version": "1.0.0",
  "description": "Aplicación de ejemplo para práctica CI/CD con Jenkins",
  "scripts": {
    "test": "node test/suma.test.js",
    "build": "echo \"Build completado\" && mkdir -p dist && cp index.js dist/"
  }
}